{
  "name": "leadcrm",
  "private": true,
  "packageManager": "npm@11.13.0",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "stripe:listen": "stripe listen --forward-to localhost:4000/api/v1/webhooks/stripe"
  },
  "devDependencies": {
    "@types/multer": "^2.2.0",
    "turbo": "^2.5.4"
  },
  "optionalDependencies": {
    "lightningcss": "^1.32.0"
  },
  "dependencies": {
    "@react-three/drei": "^10.7.8",
    "@react-three/fiber": "^9.7.0",
    "three": "^0.185.1"
  }
}
